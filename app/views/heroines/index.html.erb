<section class="hero is-primary is-bold">
  <div class="hero-body">
    <div class="container">
      <h1 class="title">
        All Superheroines
      </h1>
    </div>
  </div>
</section>

<ul>
  <% @heroines.each do |heroine| %>
    <li>
      <h3><%=link_to heroine.super_name, heroine_path(heroine)%></h3>
    </li>
    <br>
  <% end %>
</ul>
